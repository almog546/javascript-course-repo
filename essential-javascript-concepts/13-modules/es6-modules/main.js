// ES6 Modules, also known as ECMAScript modules, are the newer module system introduced in ECMAScript 2015.

import Math, { divide, multiplication } from "./math.js";

console.log(Math(1, 1))
console.log(divide(20, 5))
console.log(multiplication(5, 5))