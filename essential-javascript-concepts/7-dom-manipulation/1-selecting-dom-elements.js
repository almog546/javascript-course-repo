// Using getElementById to select the heading
const headingElement = document.getElementById("heading");
headingElement.textContent = "Selected by ID";

// Using querySelector to select the paragraph
const paragraphElement = document.querySelector(".content");
paragraphElement.textContent = "Selected by class";