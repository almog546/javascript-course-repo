// assignment
let numbers = [1, 2, 3];
numbers[0] = 69;
console.log(numbers);

// Push (add to the end):
let fruits = ["Apple", "Durian", "Banana"];
fruits.push("grape");
console.log(fruits);

// Pop (remove from the end):
let planet = ["earth", "mars", "jupiter"];
let removeElement = planet.pop();
console.log(planet);
console.log(removeElement);

// Unshift (add to the beginning):
let letters = ["a", "b", "c"];
letters.unshift("z");
console.log(letters)

// Shift (remove from the beginning):
let pets = ['dog', 'cat', 'turtle']
let removeFirst = pets.shift()
console.log(pets)

